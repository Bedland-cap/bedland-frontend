import styled from 'styled-components';

const ErrorMsg = styled.span`
  align-items: center;
  color: red;
  display: flex;
  flex-direction: row;
  gap: 5px;
  height: 2.875rem;
`;

export default ErrorMsg;
